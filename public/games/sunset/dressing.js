import * as T from 'three';
export function addStreetTrees(scene,bark){let seed=89;const random=()=>{seed=(Math.imul(seed,1664525)+1013904223)|0;return (seed>>>0)/4294967296;};const trees=[[-8.9,-9],[-8.9,19],[-8.8,-32],[9,-26],[9,22],[-8.9,-50],[9,-48],[-9,-96],[8,-98],[-3,-107],[17,-108],[-20,-110]];const wood=new T.MeshStandardMaterial({color:0x81705b,map:bark,bumpMap:bark,bumpScale:.02,roughness:1});const leafgeo=new T.BufferGeometry();leafgeo.setAttribute('position',new T.Float32BufferAttribute([0,0,0,.11,.04,.12,0,0,.28,-.11,-.015,.12],3));leafgeo.setIndex([0,1,2,0,2,3]);leafgeo.computeVertexNormals();const leaves=new T.InstancedMesh(leafgeo,new T.MeshStandardMaterial({color:0xffffff,roughness:.9,side:T.DoubleSide}),trees.length*3200);const dummy=new T.Object3D(),color=new T.Color();let count=0;
 const branch=(a,b,r)=>{const delta=new T.Vector3().subVectors(b,a),o=new T.Mesh(new T.CylinderGeometry(r*.55,r,delta.length(),8),wood);o.position.copy(a).add(b).multiplyScalar(.5);o.quaternion.setFromUnitVectors(new T.Vector3(0,1,0),delta.normalize());o.castShadow=true;scene.add(o);};
 for(const [x,z] of trees){const base=new T.Vector3(x,.15,z);branch(base,new T.Vector3(x+.1,3.8,z),.12);for(let k=0;k<6;k++){const a=k*Math.PI/3,dx=Math.cos(a),dz=Math.sin(a);branch(new T.Vector3(x,2.3,z),new T.Vector3(x+dx*1.05,3.6+random()*.6,z+dz*1.05),.055);}
 for(let i=0;i<3200;i++){let dx,dy,dz;do{dx=random()*2-1;dy=random()*2-1;dz=random()*2-1;}while(dx*dx+dy*dy+dz*dz>1);dummy.position.set(x+dx*1.8,3.9+dy*1.25,z+dz*1.6);dummy.rotation.set(random()*6.28,random()*6.28,random()*6.28);dummy.scale.setScalar(.5+random()*.45);dummy.updateMatrix();leaves.setMatrixAt(count,dummy.matrix);color.setHSL(.2+random()*.075,.35+random()*.25,.17+random()*.13);leaves.setColorAt(count++,color);}
 const border=new T.Mesh(new T.CylinderGeometry(.6,.6,.1,16),new T.MeshStandardMaterial({color:0x544a38,roughness:1}));border.position.set(x,.15,z);scene.add(border);}
 leaves.castShadow=leaves.receiveShadow=true;scene.add(leaves);
}
